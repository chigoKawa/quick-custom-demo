import { Response as Response$1, Got, ExtendOptions } from 'got';
export { HTTPError as HttpError } from 'got';
import * as debug from 'debug';

declare const createHttpClient: (configOverride?: ExtendOptions) => Got;
declare const createValidateStatusCode: (allowedStatusCodes: number[]) => (response: Response) => Response;

type HttpClient = Got;
type Response = Response$1;

/**
 * @internal
 */
declare const createLogger: (opts: {
    namespace: string;
} | {
    filename: string;
}) => debug.Debugger;
type Logger = debug.Debugger;

export { type HttpClient, type Logger, type Response, createHttpClient, createLogger, createValidateStatusCode };
