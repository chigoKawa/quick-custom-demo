// Shared TypeScript types for the Newsletter Preview app.

export interface NewsletterPreviewConfig {
  // Placeholder for configuration options (e.g. content type IDs, template selection).
  newsletterContentTypeId: string;
}

export interface NewsletterContext {
  // Placeholder for the data the app will use to render previews.
  locale: string;
}
