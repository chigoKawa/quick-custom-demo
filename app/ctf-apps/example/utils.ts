// Utility helpers for the Newsletter Preview app.
// Stubbed for now; add concrete helpers as the implementation evolves.

export function notImplemented(message = "Not implemented"): never {
  throw new Error(message);
}
